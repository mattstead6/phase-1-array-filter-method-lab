// Code your solution here
const driversInArray = drivers.filter((theDriver) => findMatching(theDriver))


function findMatching(drivers,name){
    if(drivers.name == name){
        return name
    } 
    return name
}